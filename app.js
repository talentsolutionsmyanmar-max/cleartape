const $=id=>document.getElementById(id);
function looksLikeSolana(a){return /^[1-9A-HJ-NP-Za-km-z]{32,44}$/.test(a)&&!a.startsWith('0x')}
function looksLikeEvm(a){return /^0x[a-fA-F0-9]{40}$/.test(a)}
function fmtUsd(n){if(n==null||Number.isNaN(n))return '—';const x=Number(n);if(x>=1e9)return '$'+(x/1e9).toFixed(2)+'B';if(x>=1e6)return '$'+(x/1e6).toFixed(2)+'M';if(x>=1e3)return '$'+(x/1e3).toFixed(1)+'K';return '$'+x.toFixed(2)}
function ageHours(t){return t?(Date.now()-Number(t))/36e5:null}
async function fetchDex(address){const r=await fetch('https://api.dexscreener.com/latest/dex/tokens/'+encodeURIComponent(address));if(!r.ok)throw new Error('DexScreener request failed');return r.json()}
async function fetchRugcheck(mint){const r=await fetch('https://api.rugcheck.xyz/v1/tokens/'+encodeURIComponent(mint)+'/report');if(!r.ok)return null;return r.json()}
function pairAgeLabel(h){if(h==null)return 'unknown';if(h<1)return Math.round(h*60)+' min';if(h<48)return h.toFixed(1)+' h';return (h/24).toFixed(1)+' d'}
function buildLinks(addr,chain){if(looksLikeSolana(addr))return[['RugCheck','https://rugcheck.xyz/tokens/'+addr],['Birdeye','https://birdeye.so/token/'+addr+'?chain=solana'],['Solscan','https://solscan.io/token/'+addr],['Bubblemaps','https://app.bubblemaps.io/sol/token/'+addr],['DexScreener','https://dexscreener.com/solana/'+addr]];const c=(chain||'ethereum').toLowerCase();return[['TokenSniffer','https://tokensniffer.com/token/'+addr],['Honeypot.is','https://honeypot.is/ethereum?address='+addr],['GoPlus','https://gopluslabs.io/token-security/'+addr],['Bubblemaps','https://app.bubblemaps.io/eth/token/'+addr],['DexScreener','https://dexscreener.com/'+c+'/'+addr]]}
function analyzePairs(data){const pairs=(data&&data.pairs)||[];if(!pairs.length)return{found:false,flags:['No DexScreener pair found. Confirm the contract from an official source, not a DM.'],metrics:{}};pairs.sort((a,b)=>Number(b.liquidity?.usd||0)-Number(a.liquidity?.usd||0));const p=pairs[0];const liq=Number(p.liquidity?.usd||0);const vol=Number(p.volume?.h24||0);const hours=ageHours(p.pairCreatedAt);const flags=[];let severity='warn';if(liq<10000)flags.push('Liquidity under $10k — a small sell can nuke the chart.');if(liq<1000)flags.push('Critical: liquidity is dust. Treat as hard-rug ready.');if(hours!=null&&hours<6)flags.push('Pair is under 6 hours old. Most rugs happen in the first day.');if(vol>0&&liq>0&&vol/liq>40)flags.push('Volume is extremely high vs liquidity. Wash-trading is common here.');if((p.txns?.h24?.buys||0)+(p.txns?.h24?.sells||0)<20&&hours>12)flags.push('Almost no organic flow.');if(liq<1000||(hours!=null&&hours<1&&liq<20000))severity='bad';else if(liq>=50000&&hours>=24&&flags.length===0)severity='good';return{found:true,pair:p,flags,severity,metrics:{name:(p.baseToken?.name||'Unknown')+' ('+(p.baseToken?.symbol||'?')+')',price:p.priceUsd?'$'+Number(p.priceUsd).toPrecision(4):'—',liq:fmtUsd(liq),vol:fmtUsd(vol),age:pairAgeLabel(hours),chain:p.chainId||'—',dex:p.dexId||'—',change:p.priceChange?.h24!=null?p.priceChange.h24.toFixed(1)+'%':'—'}}}
function rugFlags(report){if(!report)return[];const out=[];if(report.mintAuthority!=null&&report.mintAuthority!=='')out.push('Mint authority still exists. Deployer can print supply.');if(report.freezeAuthority!=null&&report.freezeAuthority!=='')out.push('Freeze authority still exists.');if(report.topHolders){const top=report.topHolders[0];if(top&&Number(top.pct)>20)out.push('Top holder controls more than 20% of supply.');}if(Array.isArray(report.risks))report.risks.slice(0,6).forEach(r=>{out.push((r.name||r.level||'Risk')+(r.description||r.value?' — '+(r.description||r.value):''))});if(report.score!=null)out.push('RugCheck raw score: '+report.score+' (higher usually means riskier).');return out}
async function runScan(){const raw=($('ca').value||'').trim();const box=$('result');const verdict=$('verdict');const metrics=$('metrics');const flagsEl=$('flags');const linksEl=$('tool-links');if(!raw){alert('Paste a contract / mint address first.');return}if(!looksLikeSolana(raw)&&!looksLikeEvm(raw)){alert('That does not look like a Solana mint or an EVM 0x address.');return}box.classList.add('show');verdict.className='verdict warn';verdict.innerHTML='<strong>Reading public tape…</strong><p>No wallet connect. Nothing is stored.</p>';metrics.innerHTML='';flagsEl.innerHTML='';linksEl.innerHTML='';try{const dex=await fetchDex(raw);const analysis=analyzePairs(dex);let extra=[];if(looksLikeSolana(raw)){try{extra=rugFlags(await fetchRugcheck(raw))}catch(e){extra=[]}}const allFlags=[...(analysis.flags||[]),...extra];let sev=analysis.severity||'warn';if(!analysis.found)sev='bad';if(allFlags.some(f=>/mint authority|freeze authority|dust|Critical/i.test(f)))sev='bad';verdict.className='verdict '+sev;if(!analysis.found)verdict.innerHTML='<strong>No public pair found.</strong><p>Do not buy from a screenshot.</p>';else if(sev==='bad')verdict.innerHTML='<strong>High risk on first pass.</strong><p>Stop and verify on official scanners before any size.</p>';else if(sev==='good')verdict.innerHTML='<strong>First pass is not screaming.</strong><p>Still check holders, deployer history, and LP lock.</p>';else verdict.innerHTML='<strong>Caution. Incomplete safety.</strong><p>Homework, not a green light.</p>';const m=analysis.metrics||{};const cells=[['Token',m.name||'—'],['Price',m.price||'—'],['Liquidity',m.liq||'—'],['24h volume',m.vol||'—'],['Pair age',m.age||'—'],['24h change',m.change||'—'],['Chain',m.chain||'—'],['DEX',m.dex||'—']];metrics.innerHTML=cells.map(([k,v])=>`<div class="metric"><span>${k}</span><b>${v}</b></div>`).join('');flagsEl.innerHTML=allFlags.length?('<h3>Flags from public data</h3>'+allFlags.map(f=>`<div class="flag"><div class="tag">FLAG</div><div><p>${f}</p></div></div>`).join('')):'<p class="sub">No automatic flags. That is not safety.</p>';linksEl.innerHTML=buildLinks(raw,analysis.pair&&analysis.pair.chainId).map(([n,u])=>`<a href="${u}" target="_blank" rel="noopener">${n} ↗</a>`).join('')}catch(err){verdict.className='verdict bad';verdict.innerHTML='<strong>Scanner could not finish.</strong><p>'+(err.message||'Network error')+'. Use official tools below.</p>';linksEl.innerHTML=buildLinks(raw).map(([n,u])=>`<a href="${u}" target="_blank" rel="noopener">${n} ↗</a>`).join('')}}
function scoreChecklist(){const boxes=[...document.querySelectorAll('#human-check input[type=checkbox]')];const total=boxes.length;const fail=boxes.filter(b=>!b.checked).length;const el=$('human-score');if(fail>=3)el.innerHTML=`<strong>Verdict: walk away.</strong> ${fail}/${total} checks failed.`;else if(fail>=1)el.innerHTML=`<strong>Verdict: not clear.</strong> ${fail} failed check(s).`;else el.innerHTML=`<strong>Checklist complete — still not a buy rating.</strong> Size as if it can go to zero.`}
document.addEventListener('DOMContentLoaded',()=>{$('scan-btn').addEventListener('click',runScan);$('ca').addEventListener('keydown',e=>{if(e.key==='Enter')runScan()});document.querySelectorAll('#human-check input').forEach(b=>b.addEventListener('change',scoreChecklist))});